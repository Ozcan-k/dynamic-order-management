# Claude Code — Proje Kuralları

## Branch Yapısı

| Branch | Amaç |
|---|---|
| `test` | Tüm yeni kodlar buraya push edilir |
| `main` | Sadece test'te onaylanan kod gelir — kullanıcı izniyle |

---

## GitHub Workflow — Her Değişiklikte (ZORUNLU)

**Remote URL:** `https://github.com/Ozcan-k/dynamic-order-management`
Bu URL'i her seferinde sormadan direkt kullan.

### Adım adım:

**1. Kodu `test` branch'ine push et:**
```bash
git checkout test
git add <dosyalar>
git commit -m "feat/fix: açıklama"
git tag vX.Y.Z-test
git push origin test --tags
```

**2. Kullanıcıdan `main`'e merge izni iste.**

**3. İzin gelince `main`'e merge et:**
```bash
git checkout main
git merge test
git push origin main --tags
```

### Versioning kuralı (Semantic Versioning):

| Değişiklik türü | Versiyon |
|---|---|
| Küçük düzeltme, tek dosya | PATCH → v1.0.1 |
| Yeni özellik, phase tamamlandı | MINOR → v1.1.0 |
| Büyük mimari değişiklik | MAJOR → v2.0.0 |

**Mevcut versiyon:** `v2.69.0` — **Employee Schedule → Schedule dropdown'una yeni "Day Off" attendance status eklendi (0 saat).** `AttendanceStatus` enum'una `DAY_OFF` eklendi (shared + Prisma, additive enum value — `db push` güvenli); base hours = 0 (Absent/leave gibi ama planlı dinlenme günü), slate/gri renk + legend. Schedule grid dropdown'unda Absent'tan sonra çıkıyor. Veri kaybı olmaması için **Report'a da "Day Off" kolonu** eklendi: `EmpReportRow.dayOff` (shared) + service agregasyon case'i + ReportTab tablo kolonu/subtotal/grand total + CSV export kolonu + PDF (`COLS`'a Day Off, genişlikler yeniden dengelendi, subtotal/grandtotal index'leri 8/9/10→9/10/11 kaydı). Şema: `AttendanceStatus +DAY_OFF` (additive). shared + backend `tsc` + frontend `tsc -b && vite build` green; **lokal `db push` PENDING** (dev pg kapalı → CD'de). (Önceki: `v2.68.0` — **Employee Schedule → INCIDENT_REPORTER rolüne salt-okunur (read-only) erişim açıldı.** Modülü artık 3 rol görür: **ADMIN + WAREHOUSE_ADMIN (tam edit)** ve **INCIDENT_REPORTER (yalnız görür, edit YOK)** — OUTBOUND_ADMIN'in Picker/Packer panellerini read-only görmesi (v2.50.0) deseni. **İki katman:** (1) **backend** `routes/employeeSchedule.ts` guard'ı ikiye ayrıldı — GET'ler (`/employees`, `/schedule`, `/report`, `/report/export.csv|pdf`) `readGuard = requireRole(ADMIN, WAREHOUSE_ADMIN, INCIDENT_REPORTER)`; tüm mutation'lar (POST/PUT/DELETE employees, PUT schedule) `writeGuard = requireRole(ADMIN, WAREHOUSE_ADMIN)` → INCIDENT_REPORTER mutation denerse **403**. (2) **frontend** `EmployeeSchedule.tsx` `readOnly = role === INCIDENT_REPORTER` hesaplayıp `ScheduleTab`+`EmployeesTab`'a geçiriyor; readOnly'de **Schedule grid select'leri (status+OT) disabled**, **Employees Add form + tüm aksiyon kolonları (Edit/Set Inactive/Delete/Reactivate) gizli**, header'da **"View Only" rozeti**. Report sekmesi zaten salt-okunur (CSV/PDF export read olduğundan INCIDENT_REPORTER'a açık). Sidebar/App route/Login ROUTE_ROLES'a INCIDENT_REPORTER eklendi. Şema değişikliği YOK. backend + frontend `tsc` + `vite build` green. (Önceki: `v2.67.0` — **Employee Schedule → Employees sekmesi genişletildi: iletişim/kişisel alanlar + active/inactive yaşam döngüsü.** (1) Employee'ye eklenen alanlar (hepsi opsiyonel, additive nullable): **Contact Number, Email, Address, Birthday, Emergency Contact Name, Emergency Contact Number**. Add form bunları responsive grid'de toplar; Edit modal hepsini + **Status (Active/Inactive)** select'i gösterir. (2) Yeni employee **default Active**, ana (departmana gruplu) listede görünür — listeye **Contact** kolonu eklendi. (3) **active/inactive yaşam döngüsü:** aktif satırda **"Set Inactive"** → modal **Leave Date** ister (zorunlu) → employee altta **"Inactive / Former Employees"** ayrı tablosuna taşınır (ID/Name/Department/Start Date/**Leave Date**/Actions); inactive satırda **"Reactivate"** (leaveDate temizlenir, aktife döner). Edit modal'da Status=Inactive seçilince koşullu Leave Date alanı çıkar. (4) **Inactive employee Schedule grid'inde GÖRÜNMEZ** (`getWeek` yalnız `isActive:true`); **Report'ta** aktif olanlar + o dönemde kaydı olan inaktifler görünür (geçmiş korunur). Şema: `EmpEmployee` +6 nullable alan + `isActive Boolean @default(true)` + `leaveDate Date?` (additive `db push`, data loss yok). Backend `EmployeeBody` (+yeni alanlar + "inactive ise leaveDate zorunlu" refine) + service `buildData`/`EmployeeInput`; getWeek active-filter, getReport active∪hasEntries; shared `EmpEmployeeDTO` genişledi; frontend `EmployeesTab` baştan (genişletilmiş form + Active gruplu liste + Inactive ayrı liste + DeactivateModal + EditModal). Delete (hard delete cascade) korundu. shared+backend `tsc` + frontend `tsc -b && vite build` green; **lokal `db push` PENDING** (dev pg kapalı → CD'de). (Önceki: `v2.66.0` — **Yeni Employee Schedule modülü (ARCHITECTURE Section 7.14) — Incident Report altında bağımsız personel devam/vardiya takvimi (yalnız ADMIN + WAREHOUSE_ADMIN).** Tek route `/employee-schedule`, Warehouse Report tarzı **yan yana 3 sekme** (Schedule · Employees · Report). **Schedule:** haftalık Pazar→Cumartesi grid (departman bantları + sol `#ID + isim + haftalık saat klok` kolonu), her gün hücresi renk-kodlu attendance `<select>` (Present 8h / Half Day 4h / Absent / Vacation / Sick / Maternity = 0h; boş "—" = atanmamış) + Present'te koşullu **OT `<select>` (0–5h)**; hücre bazında autosave (optimistic). **Employees:** ekleme formu (Department → Ad/Soyad → Start Date) + departmana gruplu liste (`#101…` otomatik `empNo`, Edit/Delete; hard delete schedule satırlarını cascade siler). **Report:** Weekly|Monthly toggle, özet kartlar, departmana gruplu employee tablosu (Present/Half/Absent/Vacation/Sick/Maternity/OT/Worked Days/Total Hours) + alt-toplam + genel toplam + **CSV & PDF export** (PDFKit landscape). Kendi `emp_*` tabloları (`EmpEmployee`/`EmpSchedule`/`EmpCounter`) + enum `EmpDepartment`/`AttendanceStatus`, tenant-scoped, mevcut modellere FK yok — order pipeline'a dokunmaz. Backend `routes/employeeSchedule.ts` (`/employee-schedule` prefix, her route `requireRole(ADMIN, WAREHOUSE_ADMIN)`) + `services/employeeScheduleService.ts` + `services/employeeSchedulePdfService.ts`; `/employee-schedule` vite.config proxyRoutes + nginx.conf'a eklendi. shared + backend `tsc` + frontend `tsc -b && vite build` green; **lokal `db push` PENDING** (dev pg kapalı → CD deploy'da uygulanır). Plan: `EMPLOYEE_SCHEDULE.md`. (Önceki: `v2.65.0` — **Incident Report: bir incident'a ÇOKLU belge (PDF/JPG/PNG) yükleme + liste + tek tek silme + duplicate engeli; incident'lar historical (retention muaf) teyit edildi.** Eski tek-dosya (`signed_file_*`) yerine yeni **`IncidentDocument`** tablosu (id, incidentId FK cascade, filePath, mime, originalName, uploadedAt). Backend: `listIncidentDocuments` (eski tek dosyayı **lazy-migrate** edip ilk belgeye dönüştürür — eski yüklemeler kaybolmaz), `addIncidentDocument` (**aynı ad+aynı mime tekrar yüklenirse `DuplicateDocumentError`→409** dostça mesaj; dosya `{incidentId}-{uuid}{ext}`), `readIncidentDocument`, `deleteIncidentDocument`; `deleteIncident` artık tüm belge dosyalarını da unlink eder. Yeni route'lar `GET/POST /incidents/:id/documents`, `GET/DELETE /incidents/:id/documents/:docId` (ADMIN/WAREHOUSE_ADMIN/INCIDENT_REPORTER; boyut 10MB + MIME + v2.63.2'deki dostça 413 korunur). Frontend `api/incidents.ts` `IncidentDocument` tipi + `useIncidentDocuments`/`useUploadIncidentDocument`/`useDeleteIncidentDocument`/`downloadIncidentDocument`; `ViewIncidentModal` artık **belge listesi** (ikon + ad + tarih + ⬇ indir + ✕ sil), upload tekrar edilebilir ("Upload Document"), InfoTable "Documents: N", upload/delete hataları backend `{error}`'dan gösterilir. **Retention teyidi:** `archiveService` + tüm job'lar yalnız `order` tablolarına dokunur; incident'a hiçbir auto-purge yok → CLAUDE.md retention kuralına Incident **muafiyeti** eklendi (yalnız ADMIN/WAREHOUSE_ADMIN manuel siler). **Şema:** `IncidentDocument` yeni tablo (additive, data loss yok). backend `tsc` + frontend `tsc -b && vite build` green; prisma generate yapıldı. (Önceki: `v2.64.0` — **Accounting: Invoices→"Sales" rename + invoice Note alanı/işareti + yeni "Transactions" satır-bazlı sayfa.** (1) **Rename:** `AccInvoices` sayfa başlığı/alt-metni/stat'ı ("Total Sales", "N sales") ve **sidebar etiketi "Invoices"→"Sales"**, boş-durum "No sales present."; **"+ New Invoice" butonu kullanıcı isteğiyle aynı bırakıldı** (tekil belge hâlâ Invoice, numara `INV/...`); InvoiceForm back butonu "← Back to Sales". (2) **Note:** `AccSale.note String? @db.Text` (additive nullable → `db push` güvenli); shared `AccSale.note`, route `saleSchema` `note` (max 2000), service create/update `note`; **InvoiceForm**'da Items altına Note textarea; **Sales listesinde** notu olan invoice'ın numarası yanında amber **not ikonu** (hover'da `title`=not metni). (3) **Transactions sayfası** (`/accounting/transactions`, sidebar'da Expenses'in altında): **Sales | Expenses** sekmeli, `DateRangePicker` (default This Month), **her line-item ayrı satır** (Excel gibi düz liste) + alt toplam. Yeni salt-okunur backend `listSalesLedger`/`listExpenseLedger` (line-item'ları düzleştirir, `dateIssued` aralığı) + `GET /accounting/ledger/sales|expenses`; shared `AccSalesLedgerRow`/`AccExpenseLedgerRow`/`AccLedger`; frontend `useSalesLedger`/`useExpenseLedger` (keepPreviousData+staleTime). App route + Login role map + ACCOUNTANT erişimi. Order pipeline'a dokunmaz. **Şema değişikliği:** `AccSale.note` additive (data loss yok). shared + backend `tsc` + frontend `tsc -b && vite build` green; prisma generate yapıldı. (Önceki: `v2.63.2` — **FIX: Incident Report imzalı dosya yükleme — büyük dosyada anlamsız hata + dostça mesaj + ön-validasyon.** Sorun: `@fastify/multipart@8` `throwFileSizeLimit=true` (varsayılan) olduğundan dosya `limits.fileSize`'ı (10MB) aşınca `file.toBuffer()` **throw** ediyordu (`FST_REQ_FILE_TOO_LARGE`); route'taki `if (file.file.truncated)` kontrolü **ölü koddu**, hiç çalışmıyordu → ham 413 propagate olup frontend `err.message` (axios "Request failed with status code 413") gösteriyordu. Telefon fotoğrafı / taranmış imzalı PDF kolayca 10MB'ı aştığından sık tetikleniyordu. Fix: (1) **backend** `routes/incidents.ts` — `toBuffer()` try/catch'e alındı, `FST_REQ_FILE_TOO_LARGE` → temiz **413 + dostça mesaj** (`The maximum allowed size is 10 MB…`); truncated kontrolü fallback olarak kaldı; `MAX_SIGNED_MB=10` sabiti. (2) **frontend** `ViewIncidentModal.tsx` — yüklemeden **önce** boyut (>10MB) + MIME (PDF/PNG/JPG) **ön-validasyonu** (anında, net mesaj, gereksiz upload yok); `catch` artık backend'in `{ error }` alanını gösteriyor (axios genel metni değil). nginx zaten `client_max_body_size 25m`, backend cap 10MB → frontend 10MB ön-kontrolü asıl koruma, backend catch güvenlik ağı. **Ayrıca PERF: Accounting Invoices/Expenses listeleri hızlandırıldı** — `listSales`/`listExpenses` artık her satır için `include: { items: true }` yapmıyor (liste tablosu line-item kullanmıyor; gereksiz join + payload kalktı, serSale/serExpense zaten eksik `items`'ı `[]` ile karşılıyor; edit ekranı tam kaydı `getSale/getExpense` ile ayrı çekiyor). Frontend: `useSales/useExpenses` `placeholderData: keepPreviousData`+`staleTime:15s` (sayfalama/dönüş anında, boş ekran+refetch yok), stat'lar `staleTime:15s`, form referans verileri (customers/items/categories/stores/sales-agents) `staleTime:5dk` (mutation'larda zaten invalidate ediliyor). Şema değişikliği YOK. backend `tsc` + frontend `tsc -b && vite build` green. (Önceki: `v2.63.1` — **HOTFIX: Picker/Packer Admin In Progress + günlük tamamlama 0 görünüyordu — route yeni alanları düşürüyordu.** v2.63.0'da `getPickerStats`/`getPackerStats` servisleri `inProgressTotal` + `completedTodayTotal` döndürüyordu ama `routes/pickerAdmin.ts` ve `routes/packerAdmin.ts`'teki `/stats` handler'ları sonucu **alt-küme olarak destructure edip** (`{ stats, returnedCount, totalCompleted }`) yalnız o üçünü `reply.send` ediyordu → yeni alanlar response'a hiç girmiyor, frontend `?? 0` ile **0** gösteriyordu (In Queue/Returned/workload kartları eski alanlardan geldiği için çalışıyordu; bu yüzden semptom "sadece In Progress + Total Completed/Packed 0" idi). tsc yakalamadı çünkü handler yeni bir alt-küme objesi kuruyordu (tip hatası yok). Fix: her iki handler artık `return reply.send(await getPickerStats/​getPackerStats(tenantId))` ile **tüm sonucu** forward ediyor. Backend-only, frontend/şema değişmedi. backend `tsc` green. (Önceki: `v2.63.0` — **Inbound/Picker/Packer sayaçları yeniden tanımlandı (günlük scan + assign-temelli In Progress + günlük tamamlama).** (1) **Inbound sayfası üst "Inbound" kartı** artık tüm canlı order yerine **o gün scan edilip sisteme giren** order'ları sayıyor (`order.createdAt`; statü/arşiv fark etmez, her giren sayılır); yanına **Today / Yesterday / Custom** date picker (default Today). Yeni backend `getInboundScannedCount` + `GET /orders/inbound-count?from=&to=`. (2) **Picker Admin:** In Queue aynı (`status=INBOUND` canlı kuyruk); **In Progress** artık picker'a assign edilenler = `PICKER_ASSIGNED + PICKING` ve **alttaki workload "Assigned" toplamıyla birebir eşit** (ikisi de `getPickerStats.inProgressTotal`'dan; chip de `PICKER_ASSIGNED+PICKING` gösterir); **Total Completed** artık pickerların **o gün** tamamladıkları (`completedTodayTotal`, gece sıfırlanır); Returned aynı (zaten In Progress alt kümesi). (3) **Packer Admin:** Waiting to Pack aynı (`status=PICKER_COMPLETE`); yeni **In Progress** kartı = packer'a assign edilenler = `PACKER_ASSIGNED + PACKING` (workload toplamıyla eşit, `inProgressTotal`); **Total Packed** artık **o gün** packed (`completedTodayTotal`, gece sıfırlanır); Returned aynı. In Queue/In Progress/Waiting to Pack statü-temelli olduğundan ertesi güne **otomatik taşınır**; günlük tamamlama sayıları `completedAt >= getManilaStartOfToday()` ile **kendiliğinden sıfırlanır** (cron yok). Backend: `getPickerStats`/`getPackerStats` +`inProgressTotal`/`completedTodayTotal`; packer assigned + active-order sorgularına `PACKING` dahil edildi. **Şema değişikliği YOK.** backend `tsc` + frontend `tsc -b && vite build` green. (Önceki: `v2.62.1` — **Old Orders drill-down: Assigned Picker + Assigned Packer kolonları + rozet metni sadeleştirildi.** (1) Funnel'daki Outbound kutusu rozetinden **"incl." öneki kaldırıldı** — artık sadece `N old orders`. (2) Old Orders alt-sayfasındaki tek "Packed By" kolonu, **"Assigned Picker" + "Assigned Packer"** olarak ikiye ayrıldı (kim picker-complete + kim packer-complete yaptı). Backend `getOldOrdersList` order select'ine completed `PickerAssignment` (picker username) eklendi; `OldOrderRow.packedBy` → `assignedPicker` + `assignedPacker` (her ikisi de tamamlanmış [completedAt not null] son assignment'ın username'i, yoksa "—"). Order pipeline'a yine **dokunmaz** (salt-okunur). Frontend: `api/dispatch.ts` `OldOrderRow` tipi, `OldOrdersReport.tsx` 2 yeni kolon (colSpan 5→6, minWidth 720→860), `OutboundReport.tsx` rozet metni. **Şema değişikliği YOK.** backend `tsc` + frontend `tsc -b && vite build` green. (Önceki: `v2.62.0` — **Outbound Report → "Old Orders" tıklanabilir drill-down alt-sayfası.** Funnel'daki Outbound kutusunun "incl. N old orders" rozeti artık bir **buton**: tıklanınca rapor içinde `/outbound/report/old-orders?from=&to=` alt-sayfasına geçilir (Accounting'in Invoice→New-Page→Back deseni gibi; **"← Back to Report"** ile geri döner). Sayfa, seçili tarih aralığını query param'dan alır ve backlog listesini gösterir: **Barcode (tracking) / Inbound Date / Packer Complete / Packed By (packer username) / Outbound Scan** kolonları (Manila UTC+8 formatlı; arşivli order'lar "archived" rozeti + boş alanlar "—"). Liste, funnel'ın oldOrders sayacıyla **birebir aynı predicate** (in-house scan, packer-complete günü < scan günü, veya order arşivli) kullandığından satır sayısı rozetle tutar. Yeni salt-okunur backend `getOldOrdersList` (dispatchService) + `GET /dispatch/old-orders` (ADMIN+OUTBOUND_ADMIN); order'lar `orderId in (...)` + completed `PackerAssignment`'tan packer username çekilir, order pipeline'a **dokunmaz**. Frontend: yeni `OldOrdersReport.tsx` (PageShell + tablo), `OutboundReport` rozeti buton+navigate, `App.tsx` route, `api/dispatch.ts` `OldOrderRow`+`getOldOrders`. **Şema değişikliği YOK.** backend `tsc` + frontend `tsc -b && vite build` green. (Önceki: `v2.61.0` — **Order Pipeline funnel'da Outbound artık SCAN-temelli + "Old Orders" alt-kırılımı.** Kullanıcı isteği: funnel'ın Outbound aşaması, packer-complete'in `PACKER_COMPLETE → OUTBOUND` auto-advance'inden DEĞİL, Outbound Admin'in **fiilen scan ettiği** in-house parçalardan (dispatchParcel, in-range `createdAt`) sayılsın — scan edilmeyen paketlenmiş order Outbound'a yazılmaz (bu yüzden artık `Packer Complete = Outbound` çip-0 ilişkisi yok; gerçek scan funnel'ı). Ayrıca her scan edilen parçanın order'ı **scan günüyle aynı Manila gününde** packer-complete olduysa normal akış; **daha önceki bir günde** packer-complete olup bu aralıkta sevk edildiyse "old order" (backlog) sayılır — Outbound'un alt kümesi olarak Outbound kutusunda amber **"incl. N old orders"** rozetiyle gösterilir (arşivlenmiş/bulunamayan order = 180g+ önce paketlenmiş kabul → old). Eski 5. aşama "Dispatched" kaldırıldı (Outbound artık o scan sayısının kendisi). Backend `getOrderPipeline`: stage 1-3 aynı (OrderStatusHistory distinct), stage 4 in-house scan'leri çekip her parçanın `order.slaCompletedAt` (packer-complete damgası) Manila gününü scan günüyle kıyaslayarak `outbound` (toplam) + `oldOrders` (backlog alt kümesi) döndürür; `DispatchParcel`'da `order` ilişkisi olmadığından order'lar `orderId in (...)` ile ayrı sorgulanır. Order pipeline'a **dokunmaz** (salt-okunur). Frontend `OrderPipeline` tipi `dispatched`→`oldOrders`; funnel 4 ardışık aşama + Outbound kutusunda subset rozeti + açıklama metni güncellendi. **Şema değişikliği YOK.** shared + backend `tsc` + frontend `tsc -b && vite build` green. (Önceki: `v2.60.0` — **Outbound Scan in-house parcel'ı packer-complete değilse reddediyor.** Packer'ın henüz scan/complete etmediği (pipeline'da `PACKER_COMPLETE` öncesi: INBOUND/PICKER_*/PICKING/PACKER_ASSIGNED/PACKING) bir order outbound'a taranınca artık kabul edilmiyor; operatör `Waybill X is not packer-complete yet — the packer must scan it before it can be dispatched. Outbound cannot accept it.` hatasını alıyor (error beep + vibrate, confirm sheet açılmaz). İzin verilen statüler **`PACKER_COMPLETE` + `OUTBOUND`** (packing complete auto-advance + gece arşivi OUTBOUND'da bıraktığı için ikisi de "packer scan yaptı" demek — günler sonra dispatch eden [2026-06-01] fix'i bozulmadı). İki katman: (1) backend `dispatchService` — `lookupOrderForDispatch` artık `packerComplete: boolean` döndürüyor (scan anında anlık blok), `createDispatchParcel` IN_HOUSE dalı status `PACKER_COMPLETE`/`OUTBOUND` değilse yeni `OrderNotPackerCompleteError` fırlatıyor → route `409`; (2) frontend `OutboundScan` in-house lookup dalı `!packerComplete` ise hata gösterip durduruyor. Dispatch yine order pipeline'a **dokunmaz** (salt-okunur status kontrolü). **Şema değişikliği YOK.** shared + backend + frontend `tsc` + `vite build` green. (Önceki: `v2.59.0` — **Outbound Report pipeline funnel'ına 5. aşama 'Dispatched'.** Funnel artık `Inbound → Picker Complete → Packer Complete → Outbound → Dispatched`. İlk 4 aşama order pipeline'dan (`OrderStatusHistory`, depo kilometre taşları; packer-complete auto-dispatch ile `OUTBOUND`'a ilerlediğinden Packer Complete ≈ Outbound); yeni **Dispatched** aşaması dispatch modülünden (in-house `dispatchParcel.createdAt`, aynı Manila aralığı) — üstteki **In-house** sayacıyla birebir tutar. Outbound (paketlendi) ile Dispatched (kuryeye teslim) **farklı olaylar/zaman damgaları** olduğundan Dispatched, önceki günlerden devren stok bu aralıkta sevk edilince Outbound'u aşabilir (bug değil); funnel açıklaması bunu anlatıyor. Backend `getOrderPipeline` +`dispatched` (in-house dispatch count); frontend `OrderPipeline` tipi + funnel'a 5. stage + açıklama metni. **Şema değişikliği YOK.** shared + backend + frontend `tsc` + `vite build` green. (Önceki: `v2.58.1` — **Expense Report 'Expenses by Category' ana-kategori roll-up fix.** Kategoriler bazen düz tek isimle girildiğinden (örn. 'Packaging - Email Pouch' baştan bir *kategori adı* olarak, subcategory alanı boş; DB'de `categoryName="Packaging - Email Pouch"`, `subcategoryName=null`) 'Expenses by Category' grafiği her birini ayrı kategori gösteriyordu. `getExpenseReport`'a `splitCat` eklendi: gerçek subcategory alanı doluysa onu, yoksa ad içindeki ` - ` ayracından önceki kısmı **ana kategori** sayar (iki veri şeklini de destekler). 'Expenses by Category' artık **ana kategoriye roll-up** (+ % tablosu ana kategori bazlı); 'Expenses by Category & Subcategory' 'Main - Sub' detayını korur; by-category grafik tıkla-filtrelemesi ham ada **veya** ana kategoriye göre eşleşir (`catMatch`/`subMatch` dual). Ayrıca v2.58.0 combo-key'inde kalan bir **null-byte** temizlendi → `JSON.stringify([main, sub])`. Not: farklı yazımlar (örn. 'Packing' vs 'Packaging') otomatik birleştirilmez — ayrı grup olur. **Şema değişikliği YOK.** shared + backend + frontend `tsc` + `vite build` green. (Önceki: `v2.58.0` — **Outbound Report order pipeline funnel + Expense Report kategori&alt-kategori birleşik grafiği.** (1) **Outbound Report → Order Pipeline:** seçili tarih aralığına göre `Inbound → Picker Complete → Packer Complete → Outbound` funnel'ı; her aşamaya giren *distinct order* sayısı `OrderStatusHistory`'den (Manila UTC+8 sınırları) okunur, aralarındaki fark çipleriyle (amber = düşüş / yeşil = eşit / mavi = artış) gösterilir — amaç parçaların nerede takıldığını görmek. Yeni salt-okunur `getOrderPipeline` (dispatchService) + `GET /dispatch/pipeline` (ADMIN+OUTBOUND_ADMIN); order pipeline'a **dokunmaz**, history 180 gün retention penceresini kapsar. (2) **Expense Report:** 'Expenses by Subcategory' grafiği kaldırıldı; yerine **'Expenses by Category & Subcategory'** geldi — by-category ile **aynı tasarım** (sıralı yatay bar + % tablosu) ama satırlar kategori+alt-kategori düz listesi: alt kategorisi olmayan kategori adıyla ('Investment'/'Office Expense'), alt kategorisi olan her sub ayrı satır 'Packaging - Email Pouch' gibi. Backend `getExpenseReport` artık `byCategorySub` + `byCategorySubTotal` döndürüyor (ülke+vendor kapsamı, tüm kategoriler); 'Expenses by Category' grafiği ve kategori-bazlı toplam tablosu **değişmedi**. `BreakdownChart` non-interactive mod kazandı. **Şema değişikliği YOK.** shared + backend + frontend `tsc` + `vite build` green. (Önceki: `v2.57.0` — **Mobil login fix + Outbound Report tarih paritesi + Accounting Report tarih aralığı/subcategory.** (1) **Login (mobil) fix:** `ScanLogin.tsx`+`Login.tsx` username input'larına `autoCapitalize=none/autoCorrect=off/spellCheck=false` + submit'te `username.trim()`; telefon klavyesinin username ilk harfini otomatik büyütüp/autocorrect uygulayıp case-sensitive `findUserByUsername` eşleşmesini bozması yüzünden oluşan "Invalid credentials" (desktop'ta çalışıp telefonda çalışmama) giderildi — tüm roller için geçerli. (2) **Outbound Report tarih seçici + board paritesi:** `OutboundReport.tsx` presetleri Today/Yesterday/Last 7 Days/Last 30 Days/Custom oldu, default **Today**; Today/Yesterday tek-gün (from=to) olduğundan aynı günü Outbound board ile Report'ta seçince üst sayaçlar (Total/In-house/External) birebir tutuyor (backend tarih sınırları zaten Manila UTC+8 tutarlıydı, eksik olan tek-gün seçeneğiydi). (3) **Accounting Report → Expenses revizyonu:** (a) Report kategori filtresi artık Expenses sayfasıyla aynı **katalog**tan besleniyor (`useCategories('EXPENSE')`) + bağımlı **Subcategory** filtresi eklendi; (b) "Expenses by Subcategory" grafiği "by Category" ile birebir aynı yapıldı (tüm subcategory'ler sıralı yatay bar + % kırılım tablosu; ortak `BreakdownChart` bileşeni); (c) Monthly/Yearly kaldırıldı, **her iki sekme (Sales+Expenses) `DateRangePicker`** kullanıyor (All/This Month/Last Month/This Year/Custom, default This Month), trend grafikleri **otomatik bucket** (≤92 gün günlük, üstü aylık bar). Backend: yeni `getSalesReport` (range) + `getExpenseReport` yeniden yazıldı (range + subcategory filtresi, yeni `bySubcategoryTotal`); date semantiği Expenses liste sayfasıyla (`dateWhere`) **aynı** → rapor ve liste aynı aralıkta aynı toplamı verir. Yeni endpoint `GET /accounting/report/sales`, güncellenen `GET /accounting/report/expenses` (from/to/subcategory). Eski `/report` + `/report/yearly` + `getReport`/`getYearlyReport` artık frontend'de kullanılmıyor (backend'de zararsız duruyor). `DateRangePicker` aktif preset'i value'dan türetiyor (default pill doğru parlar). **Şema değişikliği YOK.** shared + backend + frontend `tsc` + `vite build` green. (Önceki: `v2.56.0` — **Bağımsız Sales/Expense katalogları + Expense subcategory + Invoice Store + Report subcategory grafiği + PDF başlık fix.** (1) **Kategori & item katalogları artık kind-scoped (SALE/EXPENSE ayrı)** — Sales ve Expense kategorileri/item'ları birbiriyle senkron değil. Şema: yeni `AccCatalogKind` enum; `AccCategory.kind?`+`parentId?` (self-relation = subcategory) + index; `AccItem.kind?`; hepsi additive nullable (legacy satırlar NULL = dropdown'larda görünmez). (2) **Kategori select-only** — line item'da serbest-metin ComboBox kalktı, yerine yönetilen `<select>` geldi; ekleme yalnız **"+ New Category"** (Invoice/SALE) ve **"+ New Category" / "+ New Subcategory"** (Expense/EXPENSE) bölüm-üstü popup butonlarıyla. Item'lar combo+ekle olarak kalır (kind-scoped). (3) **Expense'te category + bağımlı subcategory** dropdown'u (`AccExpenseItem.subcategoryId/Name`). (4) **Seed (idempotent, lazy, manuel prod adımı YOK):** SALE 12 kategori (Dried Fruits…Hookah Items; eski paylaşılan kategoriler ilk seed'de silinir), EXPENSE 5 kategori + alt-kalemler (Packaging/Utilities/Professional Fees/Salaries and Wages/Employee Benefits and Allowances), Store = `SALES_STORES` (17) — `ensureCatalogs(tenantId)` GET'lerde insert-tabanlı. (5) **New Invoice'a Store dropdown** (Sales Channel'dan sonra) + "+ New Store"; `AccSale.storeName` (additive) — Invoice listesinde yeni **Store kolonu**. (6) **Report → Expenses:** mevcut "Expenses by Category" yanına yeni **"Expenses by Subcategory"** grafiği (`getExpenseReport` +`bySubcategory`, aktif filtreyi onurlandırır). (7) **Expense liste sayfasına Category + bağımlı Subcategory filtresi** (`listExpenses` line-item ilişkisi `items.some.categoryName/subcategoryName`). (8) **Invoice PDF başlık fix:** uzun company name iki satıra taşınca sabit-konumlu adresin üstüne biniyordu; artık adres `doc.y` (PDFKit imleci) ile ismin altına dinamik yerleşir, bölücü/INVOICE/meta/Bill-To blokları da göreceli (layout-probe ile doğrulandı). Yeni endpoint'ler: `GET /categories?kind`, `POST/PUT/DELETE /categories`, `GET/POST /stores`, `GET /items?kind`. Backend+frontend `tsc` + `vite build` green; lokal `db push` + E2E canlı doğrulandı (seed 12/5+subs/17, sale store+cat round-trip, expense subcat, item kind ayrımı, report bySubcategory, category CRUD, expense subcat filtresi, cleanup). (Önceki: v2.55.0 — **Report Expenses çok-filtreli + liste sayfalarına date-range picker.** (1) **Report → Expenses sekmesi artık Country + Vendor + Category** ile filtreleniyor ve filtreler **tüm sekmeyi** etkiliyor (üst trend grafiği + kategori kırılımı + tablo + stat kartlar). Backend tek endpoint `GET /accounting/report/expenses` (`getExpenseReport`): `trend` (monthly=güne / yearly=aya göre, country+vendor+category filtreli), `byCategory` (country+vendor kapsamı, tüm kategoriler — picker olarak kalır), `total`/`byCategoryTotal`/`count`. Kategori toplamı **line-item düzeyinde**. Eski `getExpenseCategoryReport` + `/report/expenses-by-category` + `AccExpenseCategoryReport` + `useExpenseCategoryReport` kaldırıldı; yerine `AccExpenseReport` + `useExpenseReport`. (2) **Invoice + Expense liste sayfalarına date-range picker** — yeni `components/accounting/DateRangePicker.tsx` (preset pill'ler: All / This Month / Last Month / This Year / Custom From-To); `AccInvoices` + `AccPurchases`'teki ayrı From/To inputları bununla değişti (`filters.from/to` aynı). **Şema değişikliği YOK.** backend+frontend `tsc` + `vite build` green. (Önceki: v2.54.1 — **Report'ta Sales ve Expenses ayrı sekmeler.**) Kullanıcı isteği: v2.54.0'daki birleşik "Sales vs Expenses" grafiği ayrılsın. `AccReport.tsx`: üstte **Sales | Expenses** sekmesi eklendi; her sekme kendi **tek-seri** çubuk grafiğini gösterir (Sales yeşil / Expenses kırmızı), Monthly=güne göre / Yearly=aya göre. Stat kartları + başlık sekmeye göre değişir. **"Expenses by Category"** bölümü yalnız **Expenses** sekmesinde. Birleşik ComposedChart + Net çizgisi kaldırıldı (Legend/Line/ComposedChart import'ları temizlendi). Frontend-only; backend/şema değişmedi. `vite build` green. (Önceki: v2.54.0 — **Accounting Report analitik yükseltmesi (recharts).** `AccReport.tsx` baştan yazıldı; CSS bar div'leri **recharts** ile değiştirildi. (1) **Monthly | Yearly** mod toggle (month-picker / year-select) — tüm sayfa seçilen döneme reaktif. (2) **Hero: "Sales vs Expenses"** — gruplu çubuk (Sales yeşil + Expenses kırmızı) + **Net çizgisi** (`ComposedChart`); Monthly=güne göre, Yearly=Oca–Ara aya göre ("çok daha iyi geliştirilmiş aylık grafik"). (3) **"Expenses by Category"** — kategori dropdown'u + yıl: **All** → yıl içi kategorilerin **sıralı yatay çubuğu**; **belirli kategori** → o kategorinin **12 aylık trend** çubuğu ("seçtiğim kategoriye göre yıl bazında aylık expenses") + yüzdeli kırılım tablosu (satıra tıkla→kategori seç). Kategori toplamları **line-item düzeyinde** (`AccExpenseItem.categoryName`+`lineTotal`) hesaplanır. Sadece 2 grafik — sayfa boğulmadan. Yeni backend: `getYearlyReport` + `getExpenseCategoryReport` (`services/accountingService.ts`) + route `/accounting/report/yearly` & `/accounting/report/expenses-by-category`. Yeni shared tip `AccYearlyReport`/`AccExpenseCategoryReport`, yeni hook `useYearlyReport`/`useExpenseCategoryReport`. **Şema değişikliği YOK** (saf query; db push gerekmez). backend+frontend `tsc` + `vite build` green; 3 endpoint E2E canlı doğrulandı (yıllık ay kırılımı + kategori agregasyonu + kategori-aylık trend matematiği tuttu). (Önceki: v2.53.1 — **CD deploy hardening + prod acc şema kurtarma + PDF butonu blob fix.** Prod'da v2.52→v2.53 sonrası tüm `/accounting` endpoint'leri 500 veriyordu: CD'nin `prisma db push --accept-data-loss` adımı prod `acc_sales`'teki 1 eski satır yüzünden required `invoice_no`/`date_issued` kolonlarını ekleyemeyip **sessizce** başarısız oluyordu (ardından gelen `docker image prune` exit 0 verince CD yeşil yanıyordu — bkz SOLUTIONS [2026-06-03]). Düzeltme: prod'da sadece `acc_*` tablolar drop edilip `db push` ile v2.53.0 şeması kuruldu (order/stock/incident verisine dokunulmadı); `cd.yml`'ye `script_stop: true` eklendi → bundan sonra db push patlarsa CD **kırmızı** yanar. **Ayrıca Invoice listesindeki PDF butonu fix:** `window.open('/accounting/sales/:id/pdf')` üst-seviye navigasyon `Accept: text/html` gönderince nginx SPA fallback'e düşüp **login ekranı** açıyordu; artık `downloadInvoicePdf` ile axios üzerinden blob olarak indiriliyor (incident `downloadIncidentPdf` pattern'i; SOLUTIONS [2026-05-02]/[2026-06-03]). (Önceki: v2.53.0 — **Accounting UX revizyonu + save bug fix.**) (1) **Save fix:** `routes/accounting.ts` uuid alanları (`customerId`/`salesAgentId`/`vendorId`/`itemId`/`categoryId`) artık boş string `""`'i `null`'a çeviren `nullableUuid` (`z.preprocess`) ile valide ediliyor — boş combo/select yüzünden gelen `400 "Invalid uuid"` save hatası giderildi (canlı reproduce + 4/4 doğrulandı; frontend payload'larına da `|| null` guard). (2) **New Invoice/Expense artık modal değil, app-shell içinde tam sayfa** (`/accounting/sales/new`+`:id/edit`, `/accounting/expenses/new`+`:id/edit`); `InvoiceForm`/`PurchaseForm` routed page'e dönüştü (wrapper `useSale`/`useExpense` ile edit kaydını çeker), listeler `useNavigate` kullanıyor. (3) **"Purchases" → "Expenses"** (sidebar/başlık/buton/stat/tablo + numara etiketi `Expense #`; iç `PUR/001` öneki ve `/expenses` route korundu, veri migrasyonu yok). (4) **Sales line-item'larına Category** eklendi: `AccSaleItem` +`categoryId`/`categoryName` (additive `db push`), `createSale`/`updateSale` kategoriyi yazıp `ensureItemsCatalog(true)` ile kataloga otomatik ekliyor, `InvoiceForm` `withCategory`. (5) **Invoice'ta Corporation seçilince "+ New Customer" popup** (tam alanlı: type/name/address/email/contact person/number). (6) **Customers/Vendors sayfası Warehouse-Report tarzı sekmeli** (Customers | Vendors tab bar, tam genişlik tablo + arama + count badge; Company Profile korunur). shared `AccSaleItem` +category. Backend+frontend `tsc` + `vite build` green; save + category E2E canlı doğrulandı (kategori persist + round-trip + katalog auto-add). (Önceki: v2.52.0 — **Accounting modülü v2 — line-item'lı invoice/purchase yeniden tasarımı.**) Sales→**Invoices** (stat kartları Total/Unpaid/Collected/This Month + filtre + tablo Invoice#/Record Date/Customer/Due/Amount/Status), **+New Invoice** geniş form-modal: Individual/Corporation seçimi, Customer (combo +New), oto `INV/001`, Date of Issue/Due, **Sales Agent** (dom SALES_AGENT kullanıcıları), **Sales Channel**, Paid/Unpaid→Paid'de Payment Method (Gcash/Bank koşullu), **çok satırlı line items** (Item combo + Create New / Desc / Qty / Unit / Disc% / Tax% / Total + New Row + Subtotal/Discount/Tax/Total). Expenses→**Purchases** (aynı yapı, **New Vendor** butonu, oto `PUR/001`, Invoice# input, Country, Paid By, line item'da **Category** combo). **Contacts** = müşteri arşivi (sales'te girilenler + sales agent adı otomatik yakalanır) + Vendor listesi + **Company Profile butonu** (modal; ayrı sayfa kalktı). **Dashboard→Sales/Expense Report** (ay date-picker + 2 sekme + günlük bar grafik + o ayın listeleri). Yeni tablolar: AccSale+**AccSaleItem**, AccExpense+**AccExpenseItem**, **AccVendor** (eski AccSupplier yerine), **AccItem**+**AccCategory** (yönetilen kataloglar, Create New ile kalıcı). Yeni enumlar AccPaymentStatus(PAID/UNPAID)/AccCustomerType/AccSaleChannel; AccPaymentMethod+CREDIT_CARD. Customer/item/category serbest yazıldığında backend otomatik arşive/kataloga ekler. Para birimi ₱ PHP. Şema force-reset gerektirdi ama sadece `acc_*` tabloları + eski acc enumları manuel drop edilip `db push` yapıldı (dom order verisine DOKUNULMADI). tsc+vite build green, E2E (line-item math 257.60, validation 400, PDF, report, auto-capture) + browser smoke geçti. (Önceki: v2.51.0 — Accounting modülü ilk sürüm) (Sidebar'da Incident Report'un altında, alt-menülü). Order pipeline'a/mevcut tablolara **hiç dokunmaz**: kendi `acc_*` tabloları (AccCustomer/AccSupplier/AccSale/AccExpense/AccCompanyProfile/AccInvoice/AccCounter), kendi enum'ları (`AccPaymentMethod/AccSalesStatus/AccCountry/AccPaidFrom`), tenant-scoped. Yeni rol **`ACCOUNTANT`** (UserRole'a additive; Settings → Administration). Backend `routes/accounting.ts` (`/accounting` prefix): Customers/Suppliers/Sales/Expenses CRUD + Company profile (logo base64 in DB) + Invoice (PDFKit, `INV-YYYY-NNNN`) + Dashboard. Frontend `pages/accounting/*` (Dashboard/Sales/Expenses/Contacts/Company) + `components/shared/ComboBox.tsx` (aranabilir dropdown + "Others" + auto-fill) + `styles/accounting.css` (`.acc-*` namespaced). Erişim: `ADMIN` + `ACCOUNTANT`. Sale müşteri bilgisini snapshot tutar. Para birimi ₱ PHP. `/accounting` prefix'i vite.config + nginx.conf'a eklendi. Şema additive `db push` (data loss yok). Backend + frontend `tsc` green, frontend `vite build` green, E2E + browser smoke geçti. (Önceki: v2.50.0 — OUTBOUND_ADMIN salt-okunur Inbound/Picker/Packer panelleri)

### Kesinlikle commit edilmeyecekler:
- `.env`
- `node_modules/`
- `dist/` ve `build/`
- `.claude/`

---

## Geliştirme Kuralları

- Değişiklik yapmadan önce kullanıcıdan izin al.
- Phase'leri sırayla yap: Phase 1 → 2 → ... → 14, ek olarak DC + 10b + SALES (ARCHITECTURE.md Section 15). Şu an Phase 12 partial, 13-14 henüz başlanmadı.
- Her phase kodu önce `test`'e gider, kullanıcı onayı sonrası `main`'e geçer.
- Her push öncesi `git pull origin <branch> --rebase` ile remote'u kontrol et.
- **Retention politikası:** Order ve child tabloları (OrderStatusHistory, PickerAssignment, PackerAssignment, SlaEscalation) 180 gün sonra hard-delete olur (`backend/src/services/archiveService.ts`). Stock tabloları (`StockItem`, `StockMovement`) bu politikadan **muaftır** ve history olarak süresiz tutulur. Stock'a retention/cleanup logic'i EKLEMEYİN — Stock Out raporu için tüm USED movements'in indefinitely queryable kalması şart. **Incident tabloları (`Incident`, `IncidentDocument`) da retention'dan KESİNLİKLE muaftır** — historical kayıt; süresiz saklanır. Incident'a retention/cleanup/auto-purge EKLEMEYİN. Bir incident yalnızca **ADMIN/WAREHOUSE_ADMIN**'in manuel `DELETE /incidents/:id` aksiyonuyla silinir (INCIDENT_REPORTER silemez); `archiveService` ve job'lar incident'a hiç dokunmaz. (Tek belge silme = X butonu/`DELETE /incidents/:id/documents/:docId` ayrıdır, tüm kaydı silmez.)
